import { MainLayout } from '../components/layout';
import * as React from 'react';

export interface IMusicNewProps {
}

export default function MusicNew (props: IMusicNewProps) {
  return (
    <div>
      
    </div>
  );
}
MusicNew.Layout = MainLayout;