
export default interface IsongProps {
    id: string;
    image: string;
    isRunning?: boolean;
    isYourPlaylists?: boolean;
    mvSong?: string;
    name: string;
    singer: string;
    song: string;
    viewed?: number;
    isMoreActive?: boolean;
    overlay?: boolean;
}

///useState, useEffect, useContext, useCallback, React.memo, useMemo, useRef


